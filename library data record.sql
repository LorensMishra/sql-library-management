-- View all books in the library
SELECT * FROM books;

-- View all library branches/locations
SELECT * FROM branch;

-- View all library employees/staff
SELECT * FROM employees;

-- View currently issued books (active rentals)
SELECT * FROM issued_status;

-- View all library members
SELECT * FROM members;

-- View returned books history
SELECT * FROM return_status;